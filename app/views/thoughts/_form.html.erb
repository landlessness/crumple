<%= form_for(@thought) do |f| %>
  <% if @thought.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thought.errors.count, "error") %> prohibited this thought from being saved:</h2>

      <ul>
      <% @thought.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :typing %><br />
    <%= f.text_area :typing %>
  </div>
  <div>
    <%= collection_select(:thought, :project_id, current_person.projects.all, :id, :name, :prompt => true) %>
  </div>
  <div class="actions">
    <%= f.submit 'Save' %>
  </div>
<% end %>
