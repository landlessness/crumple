= javascript ['jquery.resize-font']
#thoughts_show
  - if @thought.archived?
    = t :archived
    = link_to(t(:activate), activate_thought_path(@thought), :method => :put, :class => 'action')
  - elsif  @thought.in_drop_box?
    = t :in_drop_box
    = link_to(t(:accept), accept_thought_path(@thought), :method => :put, :class => 'action')
  #thought_body.thought-body.resize-font.people-content= display_text @thought.body
  #thought_controls.space-top
    .align-left
      = link_to t(:edit), edit_thought_path(@thought), :class=>'action'
      = link_to t(:focus), focus_thought_path(@thought), :class=>'action lil-space-left'
    .align-right
      - if @thought.archived?
        = link_to t(:activate), activate_thought_path(@thought), :method => :put, :class=>'action lil-space-right'
      -elsif @thought.in_drop_box?
        = link_to t(:accept), accept_thought_path(@thought), :method => :put, :class=>'action lil-space-right'
      -elsif @thought.active?
        = link_to t(:archive), archive_thought_path(@thought), :method => :put, :class=>'action lil-space-right'
      = link_to t(:delete), [@thought], :confirm => t(:r_u_sure).capitalize, :method => :delete, :class=>'action'
    .clear
.big-space-top
  .align-left.half
    = render 'comments'
  .align-right.half
    = render 'meta_data'
  .clear
