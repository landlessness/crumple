.prompt.lil-space-bottom
  %a{:name => 'comments'}= t(:comment).pluralize
  = %((#{@thought.comments.count}))
- form_for [@thought, @comment] do |f|
  .body-container
    = f.text_area :body, :class => 'resize-font resize-textarea body'
  .align-right.lil-space-top
    = f.submit t(:save)
  .clear
= render @thought.comments.order("created_at ASC")
