<h1>
  Listing thoughts
  <% if @tags %>
  for tags: 
  <%= @tags.join(' ')  %>
  <%= link_to 'x', thoughts_path %>
  <% end %>
</h1>
<%= link_to 'New Thought', new_thought_path %>
<%= link_to 'Archived Thoughts', archived_thoughts_path %>
<%= will_paginate @thoughts %>

<table>
  <tr>
    <th>Thought</th>
    <th>Tags</th>
    <th>Creator</th>
    <th>Project</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<%= render @thoughts %>

</table>

<br />

<%= will_paginate @thoughts %>

<%= link_to 'New Thought', new_thought_path %>
