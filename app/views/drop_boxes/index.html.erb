<h1>Listing drop_boxes</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @drop_boxes.each do |drop_box| %>
  <tr>
    <td><%= link_to 'Show', drop_box %></td>
    <td><%= link_to 'Edit', edit_drop_box_path(drop_box) %></td>
    <td><%= link_to 'Destroy', drop_box, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Drop box', new_drop_box_path %>
