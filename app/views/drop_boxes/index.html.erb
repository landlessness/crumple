<h1>Listing drop_boxes</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @drop_boxes.each do |drop_box| %>
  <tr>
    <td><%= link_to 'Show', [current_person,drop_box] %></td>
    <td><%= link_to 'Edit', edit_person_drop_box_path(current_person, drop_box) %></td>
    <td><%= link_to 'Destroy', [current_person, drop_box], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Drop box', new_person_drop_box_path(current_person) %>
