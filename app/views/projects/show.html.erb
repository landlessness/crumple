<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @project.name %>
</p>
<p>
  <b>Thoughts:</b>
  <%= link_to 'New thought about ' + @project.name, new_project_thought_path(@project) %>
  <ul>
    <%= render @project.thoughts %>    
  </ul>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
